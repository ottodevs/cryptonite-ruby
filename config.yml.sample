---

crypto:
  btc:
    amount: 1
  eth:
    amount: 100

# only fiat without fake connection
currencies: usd, eur, gbp

# basis for conversions, can be usd or btc
# default (can delete):
basis: usd

# default (can delete):
futures: false

# default (can delete):
# btc other option: bitstamp
# eth other option: poloniex (doesn't work yet)
exchanges:
  btc: coinbase
  eth: kraken
